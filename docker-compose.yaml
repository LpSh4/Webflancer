version: "3.9"

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      - PORT=3000
      - HOST=127.0.0.1
    restart: unless-stopped
    ports:
      - "2767:3000"